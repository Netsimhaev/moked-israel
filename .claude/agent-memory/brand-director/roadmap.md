# Roadmap — המוקד (שם זמני, טרם סופי)

## פרטי בסיס
עודכן לאחרונה: 2026-07-12
שלב נוכחי: בנייה — עיצוב סופי ברמה שיווקית-מכירתית הושלם על כל האתר (דף בית → קטגוריות → מוצר → checkout) → לקראת `/brand-launch`

## עיצוב סופי מכירתי — כל האתר (2026-07-12)
בוצע סבב עיצוב/שיווק מקיף על כל האתר לפי בקשת המשתמש ("רמה שיווקית גבוהה ביותר, מכירתי ביותר, מדף הבית ועד הרכישה"). **הערה טכנית חשובה שהתגלתה בסבב זה: ה-checkout אינו stub — יש אינטגרציית סליקה אמיתית מול Cardcom (`src/lib/cardcom.ts`, `api/checkout/route.ts` + `callback/route.ts`), עם flag `CARDCOM_DRY_RUN`. פער ידוע: `orderStore.ts` הוא in-memory בלבד, לא production-scale — יש להחליף ב-DB/KV לפני עומס אמיתי.** המשתמש בחר להשאיר את שכבת הסליקה כמו שהיא בסבב זה ולמקד רק בעיצוב/UX.
- **בטיחות תהליך:** אותחל git (לא היה קיים קודם) + נשמר snapshot לפני התחלת השינויים, לבקשת המשתמש — נקודת חזרה זמינה תמיד (`git log`).
- דפי קטלוג (`/locks`, `/safes/home`, `/safes/guns`): קוד משוכפל אוחד לקומפוננטה משותפת `ProductListCard` (+ `next/image`, badge "נבחר במיוחד" ל-Smart Plus, כרטיס כולו קליק, hover lift), נוספה רצועת trust badges (variant "light" חדש ל-`TrustBadge`).
- Checkout (`CheckoutFlow.tsx`): הירו הפך משטח גרדיאנט לתמונת מוצר אמיתית מסונכרנת לצבע הנבחר (`CheckoutItem.images` חדש ב-`lib/checkout.ts`).
- קורסים: עמוד hub הפך מכרטיס דל לכרטיס פיצ'ר עשיר עם סטטיסטיקות; בעמוד הקורס הוחלף בלוק גרדיאנט ריק בפאנל "מה כלול" ממשי.
- רובוטיקה: **תוקן באג אמיתי (לא רק פוליש)** — 3 תוויות הקטגוריה (רובוטיקה תעשייתית/דמויי-אדם/פלטפורמת בקרה) היו קיימות בקוד אך מעולם לא נרנדרו למשתמש (שימשו רק כ-`key`) — עכשיו מוצגות בפועל. שאר הפלייסהולדרים (מפרטים/תמונות אמיתיות) נשארו ללא שינוי כפי שסומן בקוד ("לא להמציא נתונים עד שיסופקו").
- `/campaign/michal`: נוספה תמונת אמת אחת (install ambient shot קיים) — המבנה המינימלי-חד-CTA של הדף (per wireframes.md §3) נשמר במלואו.
- דף הבית + galleries (`ProductHeroGallery`, `SafeImageGallery`, `ImageLightbox`, `CategoryCard`, `InstallShowcase`) עודכנו בסבב קודם (ראה git log) — לא נגעו בהם שוב בסבב זה.
- QA: tsc + eslint נקיים, `npm run build` production עבר נקי על כל 26 ה-routes, נבדק חזותית desktop+mobile על רוב הדפים ב-Playwright.
- **תמונות קטגוריה חדשות (AI-generated, לא צילום אמיתי):** `locks/category-hero.png`, `safes/home/category-hero.png` — הועלו ואושרו על ידי המשתמש. **החלטה מתועדת:** Techom אישרו שימוש בתמונות AI עם הלוגו שלהם לצרכי שיווק — אין צורך להסתיר/לטשטש לוגו בתמונות עתידיות מסוג זה. **תמונת "כספות נשק" נדחתה בפועל** — הצעה ראשונה הציגה אקדחים/תחמושת גלויים (נראה כמו ציוד טקטי, מנוגד למסר "בטיחות משפחתית, לא ציוד טקטי" ולמדיניות פרסום Meta/Google/TikTok) — **טרם הוחלף**, הכרטיס `/safes/guns` בדף הבית עדיין עם תמונת מוצר רגילה, ממתין לתמונה מתוקנת מהמשתמש.

## השלמת סבב product-marketing-strategy-agent + product-page-agent — רובוטיקה (2026-07-10, עודכן באותו יום)
הקטגוריה הרביעית והאחרונה שטרם קיבלה טיפול משני הסוכנים ברמת-מוצר הושלמה. **⚠️→✅ הבאג שנמצא (שני כרטיסי קטלוג מצביעים על אותו slug `industrial-line`) נפתר בפועל, לא רק תועד:** המשתמש הבהיר שאין בכלל "דגמים" בקטגוריה זו — כל פתרון רובוטי (תעשייתי או הומנואידי) נבנה בהתאמה אישית מלאה לבקשת הלקוח, **כולל התוכנה/פלטפורמת הבקרה עצמה** שהמוקד מפתחת per-client (99% מהלקוחות עסקים/חברות גדולות). לכן **`/robotics/industrial-line` הוסר לגמרי**, ו-`/robotics` אוחד לעמוד יחיד ומקיף (הירו מבהיר "לא מוכרים דגם מהמדף" → trust strip → מפרטים/תמונות כלליים כ-placeholder מפורש, ממתין לתוכן אמיתי מהמשתמש → "מי מפתח את זה" → שקיפות רגולטורית → תהליך 5 שלבים (כולל שלב חדש: פיתוח והתאמת הפלטפורמה) → FAQ 6 שאלות (כולל שאלה חדשה: "אפשר לקנות רובוט ספציפי?" → לא) → CTA+QuoteForm). קוד נבדק: tsc/eslint נקיים, `/robotics` מחזיר 200, `/robotics/industrial-line` מחזיר 404 כצפוי. שני הבריפים עודכנו בהתאם (`product-marketing-strategy-agent/robotics.md`, `product-page-agent/robotics.md`).

## שני סוכנים חדשים ברמת מוצר בודד (2026-07-08)
נוספו לצוות: `product-marketing-strategy-agent` (בונה offer טקטי — hook/דחיפות/התנגדויות/מסגור מחיר — למוצר בודד) ו-`product-page-agent` (חקר מוצר + קופי + מבנה דף, כבריף שה-web-dev-agent מיישם). שניהם רצו על כל 4 דגמי המנעולים (ALFA/Model T/Smart Plus/Smart) והפלט יושם בפועל ב-`/product/[slug]`: נוספו סעיפי "הבעיה שהמוצר פותר", יתרונות מתורגמים לתועלת, טבלת השוואת דגמים (`ModelComparisonTable`), FAQ/מענה להתנגדויות (`ProductFAQ`), הסבר תהליך הזמנה-התקנה, CTA משני לוואטסאפ, ובאדג' קטלוגי ל-Smart Plus. הוכחה חברתית הושארה כ-TODO מפורש בקוד — **לא הומצאה עדות לקוח**. כל ה-briefs נשמרו ב-`.claude/agent-memory/product-marketing-strategy-agent/` ו-`.claude/agent-memory/product-page-agent/` (קובץ לכל מוצר). tsc/eslint נקיים, כל 4 העמודים נבדקו חזותית ב-Playwright.

## הרחבת קטלוג (2026-07-06)
המותג הורחב מ-2 ל-4 קטגוריות: מנעולים חכמים, כספות (כולל נשק), **קורסים והכשרות** (חדש), **רובוטיקה מבוססת AI** (חדש — דמויי-אדם ותעשייתיים, מנוהלים בפלטפורמת בקרה שהחברה מפתחת בעצמה). `CLAUDE.md` עודכן לשקף זאת. **בהחלטת המשתמש, לא בוצע מחקר מתחרים/פרסונות/SWOT פורמלי ולא בדיקת רגולציה ראשונית לשתי הקטגוריות החדשות** — הן עברו ישירות מיצוב קליל (`positioning.md` §5) → עיצוב (`brand-identity.md` §6, `wireframes.md` §4-5) → בנייה באתר. ראה `web-dev-agent/site-structure.md` לפירוט מלא.

## ⚠️ תיקון מיצוב מהותי — מקור המנעולים החכמים (2026-07-07, עודכן 2026-07-10)
התברר בפועל שהמלאי הפיזי של המנעולים החכמים (ALFA/Model T/Smart Plus/Smart) **מיוצר על ידי Techom** — יצרן ישראלי עצמאי — לא ע"י המוקד. ~~"המוקד" הוא משווק/מפיץ מורשה שלהם~~ — **עודכן 2026-07-10, לאחר בדיקה בפועל עם עו"ד ועם Techom: הוחלט שלא להציג את המוקד כ"משווק/מפיץ מורשה" בשום מקום.** הניסוח המאושר: המוצר מיוצר על ידי Techom (פיתוח ישראלי — של Techom), והמוקד היא חברה חיצונית שבוחרת ומתקינה את המוצרים לאחר בדיקה מעמיקה של השוק, עם אחריות ושירות ישירים. **בניגוד לניסוח הקודם (2026-07-07) — כעת כן מותר לציין "פיתוח ישראלי" ביחס למוצר עצמו, ובלבד שמיוחס ל-Techom ולא למוקד.** "פיתחנו בעצמנו" עדיין נשאר תקף רק לפלטפורמת הבקרה של הרובוטיקה. נבנה קטלוג מנעולים אמיתי ב-`/locks` + 4 עמודי מוצר (`/product/alfa`, `/product/model-t`, `/product/smart-plus`, `/product/smart`) עם בורר צבעים אמיתי (תואם לצבעים שקיימים בפועל אצל Techom) ותצוגת הנחה כנה — מחיר מחירון היצרן (מאומת מ-techom.co.il, 2026-07-07) מול מחיר המוקד, **לא** מחיר "היה" בדוי. ראה `security-compliance-agent/approvals-log.md` ו-`legal-notes.md` לתיעוד המלא, ו-`strategy-agent/positioning.md` להערת התיקון. **`/brand-launch` נדחה** עד לאישור זרימת ה-QA המלאה, כולל אימות עו"ד לתנאי שימוש בתמונות/נכסי Techom.

## אינטייק ראשוני
1. **שם העסק/המותג:** "המוקד" — כיוון מוביל מ-`strategy-agent/positioning.md`, טרם סופי. 3 חלופות נוספות בקובץ (מגן-בית, נעילת-אמת, מנעול360).
2. **קטלוג מוצרים:** הכל — מנעולים חכמים, כספות (כולל כספות נשק), טווחי מחיר ראשוניים לפי `positioning.md` (מנעול חכם ~2,200-3,200 ₪ כולל התקנה; כספות במדרג בסיסי/משפחתי/פרימיום, טרם סופי).
3. **יתרון תחרותי מרכזי:** פיתוח ישראלי (טרם אומת תפעולית מול הספק בפועל — ראה סיכונים פתוחים) + התקנות מטעם החברה עצמה בפריסה ארצית.
4. **קהל יעד:** B2C בית פרטי בעיקר (פרסונה מובילה: מיכל). פרסונות משנה: אבינועם (רישיון נשק), רונן (עסק קטן) — קיימות אך לא מובילות את ההשקה הראשונית.
5. **אזורי פעילות:** ארצי, מיידי.
6. **תקציב שיווק חודשי (שלב ראשון לאחר השקה):** 5,000–20,000 ₪/חודש.
7. **יעדי זמן:** אין לוח זמנים קשיח — אתר חי בהקדם האפשרי לפי קצב העבודה בפועל.
8. **רישוי כספות נשק:** קיים היתר/תקן משטרתי מאומת — יש להציג באתר בפועל (יופנה ל-security-compliance-agent + web-dev-agent בשלב הבנייה/השקה).

## סטטוס מחלקות
| מחלקה | סטטוס | תוצר אחרון | הצעד הבא |
|-------|-------|-----------|----------|
| strategy | ✅ הושלם | `strategy-agent/positioning.md` (USP, שם, טון, תמחור-על) | עדכון אם שם/אחריות סופיים משתנים |
| research | ✅ הושלם | `research-agent/competitors.md`, `personas.md`, `swot.md`, `market-trends.md` | לתאם אישור ראשוני מ-security-compliance-agent לניסוח כספות נשק |
| design | ✅ הושלם | `design-agent/brand-identity.md`, `ui-system.md`, `wireframes.md` | להריץ `/brand-build` (אתר בפועל) |
| web-dev | ✅ MVP חי | `web-dev-agent/site-structure.md` — עמוד בית (5 קטגוריות) + מוצר + דף נחיתה + קורסים (קטלוג+עמוד) + רובוטיקה (קטלוג+עמוד B2B), ב-`site/` (Next.js) | להוסיף עמודי כספות ביתיות/נשק, לחבר CMS/מפרט אמיתי, להחליף NightHero בתצלום אמיתי, להרחיב קטלוג קורסים/רובוטים מעבר לדוגמה אחת לכל אחד |
| payments | 🟡 הוחלט, לא מומש | `payments-agent/provider-decision.md` — Cardcom נבחר, checkout הוא stub ליד בלבד | לקבל פרטי חשבון סוחר בפועל ולחבר API אמיתי |
| copywriting | ✅ הושלם (סבב 1) | `copywriting-agent/approved-copy.md` — קופי לעמוד בית/מוצר/דף נחיתה | קופי נוסף לכספות ביתיות/נשק כשייבנו עמודים |
| growth-marketing | ⬜ טרם התחיל | — | לאחר השקה |
| sales | ⬜ טרם התחיל | — | לאחר השקה |
| security-compliance | ✅ בדיקה ראשונית הושלמה | `security-compliance-agent/security-checklist.md`, `legal-notes.md`, `approvals-log.md` — אבטחת קוד תקינה, ולידציית שרת + rate limiting תוקנו בפועל, מדיניות פרסום נשק נבדקה (TikTok אסור, Meta/Google מותנה), רובוטיקה חסומה למכירה בפועל ללא ייעוץ מוסמך | HTTPS/CSRF לאמת אחרי פריסה אמיתית; לאמת עם עו"ד: רישוי הכשרות + תקן רובוטיקה ישראלי; עמודי privacy/regulation עדיין placeholder |
| qa | 🟡 ראשוני בלבד | tsc + eslint נקיים על כל 7 העמודים (כולל קורסים/רובוטיקה החדשים), נבדקו חזותית ב-Playwright, טופס ליד וטופס בקשת הצעה נבדקו מול ה-API | checklist qa-agent מלא (כולל מובייל אמיתי, Lighthouse) לפני `/brand-launch` |
| analytics | ⬜ טרם התחיל | — | לאחר תנועה אמיתית באתר |

## אבני דרך
- [x] מיצוב ואסטרטגיית מותג סגורה (positioning.md)
- [x] מחקר מתחרים, פרסונות ו-SWOT הושלמו
- [x] גודל שוק ומגמות (market-trends.md)
- [x] זהות ויזואלית נבנתה (טרם אושרה סופית ע"י המשתמש — ראה סיכונים פתוחים)
- [x] אתר חי (MVP) — בית, מוצר, דף נחיתה, ריצה מקומית תקינה (`npm run dev` ב-`site/`); טרם פרוס ל-Vercel/דומיין אמיתי
- [ ] סליקה פעילה ונבדקת (Cardcom נבחר, טרם מומש בפועל)
- [ ] קמפיין ראשון שוקק
- [ ] בדיקת רגולציה/פרסום נשק עברה אישור

## ממצא אסטרטגי חשוב (מ-market-trends.md)
זוהה חלון הזדמנות ממוקד-זמן בקטגוריית כספות נשק: זינוק במספר בעלי רישיון נשק בישראל (מכ-155K לפני 2023 לכ-215K+ בסוף 2023, ולפי דיווחים 350K+ ב-2025-2026) בשילוב תקן אחסון מחמיר חדש שנכנס לתוקף ביולי 2025 (עשוי לפסול כספות ישנות קיימות) — יוצר ביקוש כפול (רישיונות חדשים + שדרוג כספות קיימות). מספרים מדויקים טרם אומתו מול מקור רשמי — ראה `research-agent/market-trends.md`. יש לשקול העדפת קדימות לקטגוריית כספות נשק בשלב השיווק בשל תזמון השוק.

## סיכונים פתוחים
- שם מותג ("המוקד") טרם סופי — יש לבדוק זמינות דומיין (.co.il) וסימן מסחרי לפני נעילת החלטה — בעלים: משתמש — פתוח
- מספר שנות אחריות על מנעול חכם (יעד עבודה: 3 שנים) טרם אושר מול ספק/יצרן בפועל — בעלים: משתמש — פתוח
- ~~טענת "פיתוח ישראלי" חייבת אימות תפעולי~~ — **הוברר (2026-07-07):** לא תקפה למנעולים (Techom הוא היצרן, המוקד מפיץ ומתקין מורשה) — הוסרה מכל נכס שנוגע למנעולים. נשארת תקפה רק לפלטפורמת הבקרה של הרובוטיקה.
- זכויות שימוש בתמונות/נכסי שיווק של Techom — **המשתמש אישר: תמונות אמיתיות יסופקו בקרוב.** עד לקבלתן, עמודי המנעולים ממשיכים להשתמש ב-placeholder גרפי (גרדיאנט) — יש להחליף בפועל ברגע שהתמונות יתקבלו, ולוודא זכות שימוש מול הסכם ההפצה — בעלים: משתמש — פתוח
- הצגת "מחיר מחירון היצרן" (Techom) כבסיס להנחה — **המשתמש אישר להמשיך בגישה הזו** (2026-07-08), מחירי המוקד עודכנו בפועל (ALFA ₪2,300 / Model T ₪2,150 / Smart Plus ₪1,900 / Smart ₪1,550, כולם מול מחיר מחירון Techom שלא השתנה). עדיין טעון אימות עו"ד שההשוואה למחיר היצרן מותרת לפי ההסכם — ראה `security-compliance-agent/approvals-log.md` — בעלים: משתמש — פתוח
- ~~ניסוח שיווקי לכספות נשק~~ — **נבדק:** Meta/Google מותרים בזהירות (תמונות כספת בלבד, ללא נשק גלוי, מיקוד 21+), TikTok **אסור** לתוכן כספת-נשק. ראה `security-compliance-agent/legal-notes.md` — עדיין טעון אישור נקודתי לכל טקסט/תמונה בפועל לפני קמפיין.
- ~~כיוון לוגו סופי~~ — **הוכרע:** כיוון א' "האור במוקד" (עם נקודת האור הפועמת) אושר במפורש ע"י המשתמש כחזק ביותר. נותר רק לוודא התאמה אם שם המותג הסופי ישתנה מ"המוקד".
- איורים/רינדורים באתר (אם ישמשו במקום צילום אמיתי) חייבים להיות פוטוריאליסטיים, לא שטוחים/וקטוריים — נדרש לוודא בשלב web-dev/עיצוב גרפי סופי שהנכסים בפועל עומדים בכך — בעלים: design-agent/web-dev-agent — פתוח
- סיכון קוהרנטיות-מותג בין אבטחה ביתית חמה (מנעולים/כספות) לרובוטיקה תעשייתית — טופל חלקית בעיצוב (רובוטיקה לא פותחת את חוויית הבית, פלטה נשארת במשפחת הנייבי) אך לא אומת מול לקוחות אמיתיים — בעלים: משתמש — פתוח
- קורסים/הכשרות: **נבדק** — הניסוח הקיים תקין ברובו (disclaimer קיים), שיפור מומלץ (הבהרה גם בתוך תיבת "בסיום"). לא נמצא גוף ממשלתי מסמיך רשמי למנעולנות בישראל — ממצא ראשוני מ-WebSearch בלבד, **טעון אימות עו"ד סופי** — בעלים: משתמש — פתוח חלקית
- רובוטיקה: **נבדק** — אותרו תקנים בינלאומיים (ISO 10218:2025, ANSI/A3 R15.06-2025), אין אימות התאמה ישראלית ספציפית, אין עדיין תקן ISO להומנואידים (בפיתוח). **אסור למכור רובוטיקה בפועל (מעבר להצעת מחיר/ייעוץ) לפני ייעוץ הנדסי/משפטי מוסמך** — בעלים: משתמש — פתוח, סיכון משפטי פוטנציאלי, לא רק שיווקי
- אבטחת `/api/lead`: ולידציית שרת ו-rate limiting היו חסרים — **תוקן ונבדק בפועל** (400 על טלפון לא תקין, 429 אחרי 5 בקשות/דקה). ה-rate limiter הוא in-memory זמני בלבד — להחליף בפתרון משותף (Redis) לפני עומס אמיתי — בעלים: web-dev-agent — פתוח חלקית
- HTTPS/HSTS ו-CSRF על `/api/lead` — לא ניתן לאמת בסביבה מקומית; לבדוק מיד עם הפריסה הראשונה ל-Vercel/דומיין אמיתי — בעלים: web-dev-agent — פתוח
