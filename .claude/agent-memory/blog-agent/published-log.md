# יומן מאמרים שפורסמו

שורה מתוארכת לכל מאמר: slug, קטגוריה, מספר תמונות, תוצאת build, אישור compliance אם נדרש.

- **2026-07-14** — `smart-lock-israeli-door-compatibility` (קטגוריה: news) — "מנעול חכם לדלת הישראלית: 3 דברים שחייבים לבדוק לפני שקונים". 2 תמונות, שתיהן שימוש חוזר בתמונות התקנה קיימות (לא סופקו תמונות חדשות מהמשתמש בבריף): hero = `locks/model-t/install-1.jpg` (~176KB), תמונת גוף = הוחלפה לפי בקשת המשתמש מ-model-t ל-`locks/alfa/install-2.jpeg` (~33KB אחרי דחיסה) כדי להציג את דגם ALFA. `npm run build` עבר בהצלחה, `/blog/smart-lock-israeli-door-compatibility` נוצר כ-SSG. אין תוכן כספות נשק — לא נדרש אישור compliance. **הערת ניסוח חשובה:** הבריף המקורי של המשתמש השתמש בביטוי "יבואן רשמי" — תוקן במאמר הסופי לניסוח המאושר (Techom מייצרת, המוקד חברה חיצונית שבוחרת/בודקת/מתקינה/נותנת אחריות ישירה) לפי `security-compliance-agent/legal-notes.md` חלק ה. **הערה טכנית:** Next.js `_next/image` שולח cache-control ארוך-טווח לפי נתיב URL בלבד (ללא hash תוכן) — אם דורסים קובץ תמונה קיים תחת אותו נתיב אחרי שהוא כבר נצפה בפועל (לא המקרה כאן, קובץ חדש), צריך לנקות `.next/cache/images` בצד השרת וגם cache בצד הדפדפן/CDN כדי לראות עדכון.