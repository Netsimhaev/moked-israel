# מבנה האתר — "המוקד" (שם זמני)

> נבנה על ידי web-dev-agent, יולי 2026. Next.js 16 (App Router) + TypeScript + Tailwind v4, ב-`site/`. מבוסס במלואו על `design-agent/ui-system.md`, `design-agent/wireframes.md`, `design-agent/brand-identity.md`, ו-`copywriting-agent/approved-copy.md`.

## עמודים שנבנו (MVP ראשוני, לפי החלטת המשתמש)
- `/` — עמוד בית מלא: הירו עם אלמנט חתימה "אור בחלון" (`NightHero`), 5 קטגוריות מוצר (מנעולים חכמים, כספות ביתיות, כספות נשק, קורסים והכשרות, רובוטיקה), 4 עמודות בידול, 3 עדויות (לפי הפרסונות), רצועת CTA עם טופס ליד inline.
- `/locks` — קטלוג מנעולים חכמים אמיתי: 4 דגמים אמיתיים (ALFA/Model T/Smart Plus/Smart), מחיר+הנחה+צבעים לכל כרטיס.
- `/product/[slug]` (`alfa` / `model-t` / `smart-plus` / `smart`) — עמוד מוצר דינמי אחד (component משותף לפי `lib/locks.ts`, לא קוד נפרד ל-SKU), **שודרג ב-2026-07-08 על ידי `product-marketing-strategy-agent` + `product-page-agent`** לכלול: הירו+trust box+מחיר+יתרונות+טופס הזמנה (היה קיים), בתוספת: badge קטלוגי (Smart Plus), "הבעיה שהמוצר פותר", יתרונות מתורגמים לתועלת, קטע "למי זה מתאים בול" (ALFA/Smart), טבלת השוואת דגמים (`ModelComparisonTable`, מבוססת על שדה `features` מאומת), FAQ/מענה להתנגדויות (`ProductFAQ`), הסבר תהליך הזמנה-התקנה, CTA משני לוואטסאפ. הוכחה חברתית הושארה כ-TODO בקוד — **אין עדות לקוח אמיתית, לא הומצא תוכן**, ראה `.claude/agent-memory/product-page-agent/<slug>.md`.
- `/courses` (קטלוג) + `/courses/lock-installation` (עמוד קורס) — מסלול הכשרה B2B/B2C, ללא מחיר/checkout, טופס הרשמה (`LeadForm` קיים משומש חוזר), הערת מיצוב מפורשת ש"אישור השתתפות" ולא "תעודה ממשלתית".
- `/robotics` (קטלוג) + `/robotics/industrial-line` (עמוד רובוט) — מכירה B2B בלבד: אין מחיר-מדף, `QuoteForm` חדש (שם חברה/תפקיד/טלפון/היקף) עם CTA "קבעו שיחת ייעוץ" במקום "הזמינו עכשיו".
- `/campaign/michal` — דף נחיתה ממוקד-כאב לפרסונת מיכל: הדר מינימלי, CTA יחיד חוזר, 3 יתרונות ממוקדים, טופס ליד.
- `/safes/guns` (קטלוג) + `/safes/guns/[slug]` (`model-f` / `model-e`, עמוד מוצר דינמי) — **נבנה 2026-07-09, קטגוריה חדשה לחלוטין** (עד כה `#guns` היה עוגן ריק בעמוד הבית). מקור: דף המוצר הרשמי של Techom (techom-safes.co.il) לכל דגם, לא קטלוגי ה-PDF של המנעולים. `lib/safes.ts` — מודל דאטה עצמאי (`GunSafeProduct`, לא `LockProduct`): לכל דגם מחיר משלו מול מחירון היצרן, **משלוח (₪90) והתקנה (₪350) כתוספות נפרדות בשני הדגמים — בניגוד למנעולים, מחיר המוצר כאן אינו כולל התקנה**, מפרט טכני קשיח (מידות/משקל/עובי/בריחים/עיגון), ושדה `features` (`SafeFeatures`: fingerprint/digitalCode/mechanicalKey/batteryPowered) המזין `SafeComparisonTable.tsx` (נוסף 2026-07-09 עם מודל E, מקביל ל-`ModelComparisonTable` אך עצמאי כי מבנה ה-features שונה לגמרי). טענת תקן/רישוי מיוחסת ליצרן ("לפי הצהרת היצרן") בכל דגם, טעונה אישור עו"ד לפני קמפיין ממומן (`security-compliance-agent/approvals-log.md`). **מודל F:** פתיחה במפתח מכני בלבד, ללא סוללות (ראה הערת קוד ב-`lib/safes.ts` על סתירה שנפתרה מול המשתמש — דף Techom המקורי הזכיר גם סוללות בטעות). **מודל E (נוסף 2026-07-09):** 3 אמצעי גישה — טביעת אצבע, קודן דיגיטלי, מפתח מכני כגיבוי, פועלת על 4 סוללות AA (עקבי, ללא סתירה). `ProductOrderForm`/`PriceDisplay` הורחבו (backward-compatible) לתמוך גם במוצר בלי צבעים ובהערת מחיר מותאמת. **ארכיטקטורה:** בסבב הראשון (מודל F לבדו) ה-route היה סטטי ייעודי (כמו `courses/lock-installation`); עם הוספת מודל E הומר ל-**route דינמי `[slug]`** (כמו `/product/[slug]` של המנעולים) עם `generateStaticParams`, כדי לא לשכפל קוד — ראה `product-page-agent/model-f.md`/`model-e.md` לפירוט המלא. `Header.tsx` ועמוד הבית עודכנו לקשר ל-`/safes/guns` במקום `/#guns`. עדות ה"אבינועם" בעמוד הבית עודכנה לניסוח תקני מדויק יותר (עדיין placeholder, לא לקוח אמיתי).
- `/privacy`, `/regulation` — עמודי placeholder (טקסט משפטי/רגולטורי טרם סופי, טעון security-compliance-agent).
- `/api/lead` — נקודת קצה POST ללכידת לידים/פניות B2B (ראה payments-agent/provider-decision.md — אין checkout אמיתי עדיין, זהו stub שרושם ל-log בלבד; יש לחבר ל-CRM בפועל). משמש גם את `QuoteForm` (עם `source` שונה לזיהוי פניות רובוטיקה).

## הרחבת קטלוג (קורסים + רובוטיקה)
- `CategoryCard` (`src/components/CategoryCard.tsx`) הורחב מ-3 ל-5 ערכי `Category`, כולל גרדיאנטים חדשים לפי `design-agent/brand-identity.md` §6 (קורסים: חם-נחושת; רובוטיקה: נייבי-כהה, לא אפור-קר, כדי לא לשבור טון המותג).
- קופי לקטגוריות החדשות נכתב ישירות בקוד (לא דרך copywriting-agent — לא הופעל מחדש בסבב הזה לפי החלטת המשתמש לדלג על שלבי מחקר/תוכן מלאים); יש לסמן זאת אם יורחב בהמשך.
- לא בוצע מחקר מתחרים/פרסונות/SWOT פורמלי לשתי הקטגוריות (החלטת משתמש מפורשת) — ראה `strategy-agent/positioning.md` §5 להערת המתודולוגיה.

## מערכת עיצוב מיושמת
- טוקנים (`globals.css`): כל צבעי `brand-identity.md` כ-CSS variables + Tailwind `@theme inline`.
- גופנים: Frank Ruhl Libre / Heebo / Rubik דרך `next/font/google` (self-host אוטומטי, אין תלות ברשת חיצונית בזמן ריצה).
- רכיבים תחת `src/components/`: `Logo` (מוטיב "אור פועם" מאושר), `NightHero` (אלמנט חתימה — **CSS placeholder בלבד, מסומן קוד + UI note לפני production**, ראה brand-identity.md §4-5), `Button`, `TrustBadge`, `CategoryCard`, `DiffItem` (כולל `IsraelFlagIcon` — SVG במקום אמוג'י דגל, שלא נתמך בעקביות בכל הדפדפנים/מערכות), `Testimonial`, `LeadForm`, `QuoteForm` (B2B, רובוטיקה), `PriceDisplay` (מחיר מחירון מול מחיר המוקד — לא הנחה בדויה), `ColorVariantSelector`, `ProductOrderForm`, `ModelComparisonTable` (משותף לכל 4 דפי המנעולים, מבוסס על `LockProduct.features`), `ProductFAQ` (accordion, `<details>` פשוט), `Header`, `Footer`.
- `src/lib/locks.ts` — מקור אמת יחיד לקטלוג המנעולים (4 מוצרים אמיתיים, Techom): מחיר, צבעים, `features` (flags מאומתים בלבד — לא להוסיף ללא מקור), `problemStatement`, `benefitTranslations`, `faq`, `bestFor`/`catalogBadge` אופציונליים. **עודכן 2026-07-08 (סבב 1):** התקבל קטלוג PDF רשמי של Techom ל-ALFA בלבד — נוספו `LockFeatures.palmId` ו-`LockFeatures.alarm`, ו-ALFA עודכן ל-`mechanicalBackup: true` + `card: true` (מפתחות פיזיים לגיבוי וצ'יפ, מאומתים בקטלוג). `ModelComparisonTable` עודכן עם שורות חדשות (זיהוי כף יד, אזעקה מובנית).
  **עודכן 2026-07-08 (סבב 2):** התקבל קטלוג PDF רשמי גם ל-Model T. Model T הפך כמעט זהה ל-ALFA ביכולות (כל 9 דגלי `features` כעת `true`) — 7 אמצעי גישה (כולל זיהוי כף יד, לפי אישור מפורש של המשתמש בשיחה ולא ציטוט מילולי מהקטלוג, שמפרט "שישה אמצעי גישה" בטקסט), עד 150 משתמשים (טקסט חופשי, לא שדה טיפוס), מצלמה+וידאו LIVE, צג פנימי, פעמון חכם, אזעקה מובנית, כפתור שבת, עמידות מזג אוויר, סוללת ליתיום עם התרעות עבריות. **תופעת לוואי:** מכיוון ש-ALFA איבד את בלעדיות "זיהוי פנים+מצלמה", עודכנו גם `bestFor` וה-`faq` של ALFA (USP חדש: ללא הגבלת משתמשים מול 150 ב-Model T) — ראה `product-page-agent/alfa.md` ו-`product-marketing-strategy-agent/alfa.md` להיסטוריה המלאה. Smart Plus ו-Smart עדיין `palmId: false`/`alarm: false` — קטלוגיהם טרם התקבלו, לא שונו.
  **עודכן 2026-07-08 (סבב 3):** התקבל קטלוג PDF רשמי גם ל-Smart Plus. Smart Plus התברר קרוב מאוד ל-ALFA/Model T ביכולות טכנולוגיות (מצלמה HD+וידאו LIVE, מסך פנימי, אזעקה מובנית, כפתור שבת, 5 אמצעי גישה: טביעת אצבע/קוד/כרטיס/אפליקציה/מפתחות לגיבוי) — **אך במפורש וללא זיהוי פנים וללא זיהוי כף יד** (הבחנה מפורשת מהמשתמש בשיחה, לא הסקה). `features`: `fingerprint/camera/card/app/mechanicalBackup/shabbatMode/alarm` → `true`; `faceId`/`palmId` נשארו `false` — מכוונים ומאומתים, לא ברירת מחדל שלא עודכנה. `ModelComparisonTable` יציג ✗ אמיתי בשתי השורות האלה עבור Smart Plus. Smart עדיין `palmId: false`/`camera: false`/`alarm: false` וכו' — קטלוג טרם התקבל, לא שונה.
  **תוקן 2026-07-08 — באג בטבלת השוואת דגמים:** המשתמש זיהה ש-`ModelComparisonTable.tsx` מיזג בטעות שתי תכונות שונות לשורה אחת ("זיהוי פנים + מצלמה בדלת" תחת מפתח `faceId` בלבד) — כך ש-Smart Plus (יש לו מצלמה, אין לו זיהוי פנים) הוצג בטעות עם "—" כאילו גם אין לו מצלמה. תוקן: הפרדה לשתי שורות עצמאיות — "זיהוי פנים" (`faceId`) ו"מצלמה בדלת" (`camera`, שדה קיים שלא היה מוצג כלל בטבלה קודם). אומת מול הדאטה: ALFA/Model T/Smart Plus כולם `camera: true`; רק ALFA/Model T הם `faceId: true`; Smart היה `false` בשניהם באותו שלב (קטלוג טרם התקבל).
  **עודכן 2026-07-08 (סבב 4, קטלוג רביעי והאחרון):** התקבל קטלוג PDF רשמי גם ל-Smart. Smart התברר עשיר משמעותית ממה שתועד ("קוד אישי בלבד") — כולל בפועל **טביעת אצבע**, קוד, צ'יפ/מפתחות, אפליקציה (כולל שליטה מרחוק), קודים זמניים, **אזעקה מובנית נגד פריצה**, נעילה אוטומטית, פעמון מובנה, עברית מלאה, גוף אלומיניום/פלדת אל-חלד, סוללה נטענת בכבל Type-C. `features`: `fingerprint/card/app/mechanicalBackup/alarm` → `true`; `faceId/palmId/camera/shabbatMode` נשארו `false` — מאומת ישירות מהקטלוג (אין מצלמה/מסך/ביומטריה מתקדמת/כפתור שבת ברשימת "מאפיינים בולטים" של הקטלוג). `bestFor` של Smart עודכן מהיסוד כי הישן טען בטעות "קוד בלבד". **כל 4 קטלוגי Techom (ALFA/Model T/Smart Plus/Smart) התקבלו כעת — אין עוד מוצר בקטגוריית המנעולים שממתין לקטלוג.**
  **עודכן 2026-07-08 (סבב 5) — USP חדש ל-ALFA: סורק פנים+כף יד מתכוונן לגובה המשתמש.** נוסף שדה `heightAdjustableScanner` ל-`LockFeatures` (`true` ל-ALFA בלבד, `false` לשאר 3) ושורה חדשה ל-`ModelComparisonTable.tsx`. המקור הוא אישור מפורש של המשתמש בשיחה, **לא** קטלוג ה-PDF. עודכנו גם `tagline`/`description`/`highlights`/`benefitTranslations`/`bestFor`/`faq` של ALFA, ו-FAQ המקביל ב-Model T ("מה ההבדל בין Model T ל-ALFA"). **החלטת ניסוח:** נעשה שימוש ב"הדגם היחיד בקטלוג שלנו" (ניתן לאימות עצמי מהדאטה) ולא ב"היחיד בישראל" (טענת שוק רחבה שהמשתמש אישר אך לא גובתה במקור חיצוני) — טענת השוק הרחבה דורשת אישור security-compliance-agent לפני שימוש בפרסום ממומן. גם CLAUDE.md נבדק מחדש: "פיתוח ישראלי" **לא** נוסף לניסוח (המשתמש אישר שהכוונה היא ש-Techom, היצרן, הוא ישראלי — עובדה שכבר מוצגת באתר — לא שהמוקד פיתח את המנעול).
  **עודכן 2026-07-08 (סבב 6) — עדכון צבעים לכל 4 הדגמים לפי הנחיית המשתמש.** `lockColors` ב-`lib/locks.ts` הוחלף: הוסרו `goldRed`/`silver` (לא בשימוש עוד), נוספו `black` (#161616), `roseGold` (#b76e79), `white` (#f5f3ef); `blue` שונה שם ל-`blueMatte` (label "כחול מאט", אותו hex). צבעים חדשים לכל דגם: **ALFA** — רוזגולד, שחור (היה צבע יחיד אפור-שחור; עכשיו 2 צבעים). **Model T** — כחול מאט, אפור-שחור (ללא שינוי במהות, רק תיוג "מאט"). **Smart Plus** — אפור-שחור, לבן מלא, רוזגולד (הוחלף מ-זהב-בורדו/אפור-שחור/כסף). **Smart** — אפור-שחור, לבן מלא, רוזגולד (זהה ל-Smart Plus, הוחלף מ-זהב-בורדו/אפור-שחור). `ColorVariantSelector.tsx` גנרי לחלוטין, לא נדרש שינוי קוד שם. המשתמש ציין שישלח בהמשך תמונות אמיתיות לכל דגם/צבע — עדיין ממתינים, אין תמונות מוצר אמיתיות באתר (גרדיאנט placeholder בלבד).
- RTL: `<html lang="he" dir="rtl">` ברמת ה-layout הגלובלי.

## סטטוס QA ראשוני שבוצע בפועל
- `tsc --noEmit` — עבר ללא שגיאות.
- `next dev` — כל 3 העמודים הראשיים מחזירים 200, נבדקו חזותית בדפדפן אמיתי (Playwright): RTL תקין, גופנים נטענים, אלמנט האור הפועם מוצג נכון בלוגו ובהירו.
- תוקן באג: אייקון אמוג'י דגל ישראל (🇮🇱) לא נרנדר בסביבת הבדיקה (Windows) — הוחלף ב-SVG מוטבע.
- טופס ליד נבדק מול `/api/lead` בפועל — מחזיר 200 ושומר לוג.
- **טרם בוצע:** QA מלא (checklist qa-agent), בדיקת מובייל אמיתית (רק תבנית responsive נכתבה, לא נבדקה בעין על מסך צר), Lighthouse/PageSpeed, ותוכן מפרט טכני מספרי אמיתי.

## פערים ידועים לפני שקה
- Checkout/סליקה בפועל (Cardcom) — לא מומש, רק stub ליד. ראה `payments-agent/provider-decision.md`.
- `NightHero` הוא placeholder CSS/SVG, לא תצלום/רינדור פוטוריאליסטי — **חובה להחליף לפני שקה** לפי brand-identity.md.
- מפרט טכני של המוצר מציג "לפי דגם" placeholder, לא נתונים אמיתיים.
- עמודי privacy/regulation הם placeholder טקסטואלי בלבד.
- ~~כספות ביתיות עדיין מקושרות רק לעוגן~~ — **נבנה 2026-07-09.** `/safes/home` (קטלוג) + `/safes/home/[slug]` (`ib250`/`sc250`/`bsa250`, דינמי מהיום הראשון — לא סטטי-ואז-הומר כמו כספות הנשק, כי 3 הדגמים הגיעו יחד). `lib/homeSafes.ts` — מודל דאטה עצמאי (`HomeSafeProduct`), **ללא** שדה מקביל ל-`standardNote` של כספות הנשק (אין רגישות רגולטורית של נשק כאן). שלושת הדגמים מאותו גוף פיזי בדיוק (25×35×20 ס״מ, 6.5 ק״ג, 1 מ״מ/3 מ״מ פלדה) — ההבדל היחיד הוא שיטת פתיחה: IB250 (קוד+מפתח, ₪390), SC250 (+מסך LCD, ₪550), BSA250 (+טביעת אצבע, ₪650). **מחירים נשארו זהים למה שמוצג באתר Techom** — לפי הוראה מפורשת של המשתמש, בניגוד לכספות הנשק שם המחירים נקבעו עצמאית. משלוח ₪42 (לא ₪90 כמו כספות נשק — משקל קל יותר), התקנה ₪350. `HomeSafeComparisonTable.tsx` — קובץ נפרד מ-`SafeComparisonTable`/`ModelComparisonTable` (features שונים: כולל `lcdScreen`). `Header.tsx`/עמוד הבית עודכנו לקשר ל-`/safes/home` במקום `/#safes`. **פתוח:** המשתמש ציין "4 דפי מוצר" אך שלח 3 קישורים — יש לברר אם נשמט דגם רביעי.
- **עודכן 2026-07-09 (סבב שני) — נוספו B600 ו-Model 3 ל-`homeSafeProducts`.** קטגוריה עברה מ-3 ל-5 דגמים בלי כל שינוי ב-`page.tsx`/`HomeSafeComparisonTable` (data-driven כמתוכנן) — רק `lib/homeSafes.ts` עודכן, ושורה אחת ב-`[slug]/page.tsx` להעברת `colors={product.colors}` ל-`ProductOrderForm` (שדה חדש `colors?: LockColor[]` על `HomeSafeProduct`, משתמש חוזר בטיפוס `LockColor` מ-`lib/locks.ts` דרך `import type`). **B600** — טייר עסקי/כבד (64 ק"ג, דלת 10 מ"מ, ₪2,400/₪2,800). **Model 3** — טייר מעוצב/יוקרתי, ציפוי עור, 4 גווני צבע (ראשון בקטגוריה עם וריאציית צבע), **סוללת ליתיום נטענת מובנית ולא AA כמו כל שאר הדגמים** — הובלט בקוד ובקופי במכוון, ₪2,800/₪3,300. גוף/דלת/בריחים/עיגון לא צוינו במקור עבור Model 3 — הושמטו מטבלת ה-`specs`, לא הומצא ערך. סתירת מקור (גיבוי מפתח מכני ב-Model 3, 2 fetches סתרו זה את זה) נפתרה ב-fetch שלישי ממוקד על שורת "פתיחה" בטבלת המפרט בלבד — מתודולוגיה מתועדת בהערת header של `lib/homeSafes.ts` כתקדים לעתיד. **מחיר הונח שוב (לא אושר מחדש השבוע) שנשאר כמו Techom** — לוודא מול המשתמש בפעם הבאה. וידאתי בקוד ש-`ColorVariantSelector` ברירת המחדל היא תמיד `colors[0]` (לבן ל-Model 3) — לא נמצא באג, תצפית קודמת על "אדום כברירת מחדל" הייתה קריאה שגויה של צילום מסך.
- **עודכן 2026-07-09 (סבב רביעי) — תמונות מוצר אמיתיות ראשונות באתר: ALFA.** המשתמש שלח 6 קבצי תמונה אמיתיים מ-Google Drive (2 "תמונה ראשית" ברקע לבן, לפי צבע; 4 תמונות התקנה אמיתיות על דלתות לקוחות). נשמרו ב-`site/public/images/locks/alfa/`, כווצו ב-`sharp` (PNG 6.8MB→JPEG 111KB). **תבנית חדשה שנוספה לכל 4 קבצי המוצר (`lib/locks.ts`, `lib/safes.ts`, `lib/homeSafes.ts` יכולים לאמץ אותה בעתיד):** `images?: Record<string,string>` (colorId→נתיב "תמונה ראשית") ו-`installPhotos?: string[]` על `LockProduct`. **נוצר `ProductHeroGallery.tsx`** — client component חדש שמאחד את עמודת התמונה+עמודת ה-buy box תחת state צבע משותף אחד, כדי שהתמונה הראשית תתחלף בפועל כשהלקוח בוחר צבע (בעיה: `ColorVariantSelector` היה קבור בתוך `ProductOrderForm`, ושתי העמודות היו תחת server component page.tsx בלי דרך לשתף state). `ProductOrderForm` קיבל prop אופציונלי חדש `selectedColor` (controlled) — כשמועבר, מדלג על רינדור הבורר הפנימי שלו ומשתמש בערך שהתקבל; **backward-compatible** — כל שאר הקטגוריות (courses/home-safes/robotics/safes) ממשיכות להעביר רק `colors` ולא נשברות. אומת חזותית ב-Playwright: מעבר רוזגולד↔שחור מחליף בפועל את התמונה הראשית. `product/[slug]/page.tsx` פושט: כל בלוק ה-grid הישן (תמונה+trust box מול badge/title/price/highlights/order-form) הוחלף בקריאה אחת ל-`<ProductHeroGallery product={product} discountPercent={...} />`. **שאר 3 דגמי המנעולים (Model T/Smart Plus/Smart) וכל שאר הקטגוריות עדיין gradient placeholder** — יוחלפו כשיתקבלו תמונות דומות, ראה `product-page-agent/alfa.md` עדכון 4.
- **עודכן 2026-07-10 (סבב שישי) — תמונות מוצר אמיתיות + lightbox הורחבו לקטגוריית כספות הנשק (מודל E, מודל F).** בקשת המשתמש: "בדיוק כמו שעשית למנעולים". **הבדל ארכיטקטוני מכוון מהמנעולים:** לכספות נשק אין וריאציית צבע (גוון אפור יחיד) ואין "תמונות התקנה על דלת" (כספת עומדת/מעוגנת לקיר, לא מנעול-דלת) — לכן נבנתה קומפוננטה ייעודית `SafeImageGallery.tsx` (hero לחיץ + רצועת thumbnails מגלריית זוויות סטודיו, לא "צבע→תמונה" כמו `ProductHeroGallery`). כדי לא לשכפל קוד, ה-lightbox עצמו חולץ מתוך `ProductHeroGallery.tsx` (היה שם `InstallPhotoLightbox` מקומי) לקובץ משותף חדש `ImageLightbox.tsx`, וכעת שתי הקומפוננטות (מנעולים+כספות) משתמשות באותו lightbox. `lib/safes.ts`'s `GunSafeProduct` קיבל שדה חדש `images?: string[]` (מערך מסודר, `images[0]`=hero). מודל E: 4 תמונות (hero + 3 זוויות, כולל 2 עם דלת פתוחה שמראות מדף פנימי ובריחים). מודל F: 2 תמונות בלבד (hero + זווית דלת פתוחה שמראה בבירור שאין רכיב אלקטרוני — תואם לטענת "מפתח מכני בלבד"). `safes/guns/page.tsx` (קטלוג) עודכן להציג `p.images?.[0]` אם קיים, אחרת גרדיאנט — אותה תבנית בדיוק כמו `locks/page.tsx`. תמונות נשמרו תחת `site/public/images/safes/guns/{model-e,model-f}/`; רוב הקבצים כבר הגיעו קטנים מספיק (webp) ולא נדרש כיווץ נוסף, למעט 2 קבצי מודל E שכווצו ל-1200px. אומת: `tsc`/`eslint` נקיים, שני עמודי המוצר + הקטלוג נבדקו חזותית ב-Playwright כולל lightbox פונקציונלי. ראה `product-page-agent/model-e.md`/`model-f.md` עדכוני 2026-07-10 לפירוט המלא.
- **עודכן 2026-07-10 (סבב שביעי) — תמונות מוצר אמיתיות הורחבו לכל 4 כספות הבית שהיו על placeholder (IB250, SC250, BSA250, B600).** אותו תהליך בדיוק כמו כספות הנשק: `HomeSafeProduct` קיבל שדה חדש `images?: string[]` (מערך מסודר, `images[0]`=hero) — לא נדרשה קומפוננטה חדשה, `SafeImageGallery.tsx` נעשה בו שימוש חוזר מלא (רק prop חדש `fallbackGradient` כדי לתת לכספות בית גרדיאנט ירוק-כהה במקום החום של כספות נשק). כל תמונה מכל 4 התיקיות עברה בדיקה חזותית מלאה (Read tool) לפני שילוב. B600 כלל תמונת "detail" נוספת (מנעול-משנה פנימי עם מפתח נפרד) שנשמרה כתמונת גלריה, לא hero. `safes/home/page.tsx` (קטלוג) עודכן לאותה תבנית `p.images?.[0]` שכבר קיימת ב-`locks/page.tsx` ו-`safes/guns/page.tsx`. Model 3 (הדגם החמישי בקטגוריית כספות הבית) נשאר על גרדיאנט placeholder — לא סופקו תמונות עבורו. אומת: `tsc`/`eslint` נקיים, כל 5 עמודי הקטגוריה (`/safes/home` + 4 דפי מוצר) מחזירים 200, ולייטבוקס נבדק חזותית ב-B600. ראה `product-page-agent/home-safes.md` עדכון 2026-07-10 לפירוט המלא.
- **עודכן 2026-07-10 (סבב שמיני) — סט תמונות חדש ומלא למודל F (5 תמונות, החליף את הזוג המקורי) + הורדת Model 3 מהאתר.** (1) מודל F: המשתמש שלח תיקיית Drive חדשה עם 5 קבצי PNG; הוחלפו `hero.webp`/`angle-2.webp` הישנים ב-`hero.jpg` עד `angle-5.jpg` (5 תמונות, `lib/safes.ts` עודכן). אחת מהתמונות הגיעה עם רקע שקוף — נדרש `.flatten({background:"#ffffff"})` ב-sharp לפני JPEG כדי למנוע רקע שחור לא עקבי; תיעוד מלא ב-`product-page-agent/model-f.md`. (2) Model 3 (כספת בית מעוצבת) הוסר מ-`homeSafeProducts` ב-`lib/homeSafes.ts` לפי בקשת המשתמש ("תוריד כרגע") — הוסר גם `model3Colors` (הפך unused). אובייקט המוצר המלא תועד ב-`product-page-agent/home-safes.md` להשבה עתידית, כי אין git בפרויקט הזה. קטלוג `/safes/home` יורד מ-5 ל-4 דגמים; `/safes/home/model-3` מחזיר 404 (data-driven, לא נדרש שינוי ב-`[slug]/page.tsx`). אומת: `tsc`/`eslint` נקיים, כל העמודים הרלוונטיים נבדקו (200/404 לפי הצפוי), גלריה+לייטבוקס של מודל F נבדקו חזותית ב-Playwright.
- **עודכן 2026-07-10 (סבב תשיעי) — checkout אמיתי + סליקת Cardcom נבנו לכל 11 המוצרים באתר, כולל order-bump cross-sell.** בקשת המשתמש: לחבר סליקה אמיתית בדפי מוצר (מנעולים/כספות/קורס בנפרד, לא עגלה כללית), checkout שיווקי-מכירתי עם כפתור CTA ברמה גבוהה, ו-cross-sell של 5% הנחה על מוצר משלים באותו מסך תשלום. **קוני הקורס לא רואים cross-sell כלל** (הוחלט במפורש). שכבת דאטה חדשה: `lib/courses.ts` (מוצר קורס יחיד, ₪790 — לא היה שדה מחיר לקורס בכלל לפני כן), `lib/checkout.ts` (`getCheckoutItem(slug)` — נקודת נורמליזציה יחידה מעל 4 מקורות דאטה שונים: מנעולים/כספות נשק/כספות בית/קורס, כל אחד עם צורת תמחור שונה), `lib/crossSell.ts` (מיפוי קבוע 10/11 slugs, מנעולים↔כספות בלבד, זיווג לפי טווח מחיר), `lib/format.ts` (`formatPrice` — helper משותף חדש, לא הוחל למפרעה על קוד קיים). `BuyNowButton.tsx` (CTA חדש) נוסף מעל טופס הליד הקיים (`ProductOrderForm`/`LeadForm`, **לא נמחק** — נשאר כאופציה משנית "יש לי שאלה לפני שרוכשים") בכל 4 דפי המוצר (מנעולים דרך `ProductHeroGallery.tsx`, כספות בית/נשק ישירות ב-`[slug]/page.tsx`, קורס ב-`courses/lock-installation/page.tsx` שגם קיבל תצוגת מחיר לראשונה). Route חדש `/checkout/[slug]` (יחיד לכל 11 ה-slugs, ללא prefix קטגוריה — כולם ייחודיים גלובלית) + `components/checkout/CheckoutFlow.tsx` (client component: הירו לפי קטגוריה בצבעי המותג הקיימים, תגי אמון, סיכום הזמנה שקוף עם פירוק מחיר/משלוח/התקנה, order-bump checkbox חי, טופס פרטים כולל אימייל — שדה חדש שלא נאסף באתר לפני כן). API חדש: `/api/checkout` (מחשב מחיר **בשרת בלבד**, דוחה תקיפת cross-sell מזויף עם 400 — נבדק בפועל), `/api/checkout/callback` (webhook, מאמת מול Cardcom עצמו לפני סימון הזמנה כמושלמת, אידמפוטנטי). `lib/cardcom.ts` מרכז את **כל** ההנחות הלא-מאומתות על מבנה ה-API של Cardcom (מסומנות `// TO VERIFY` בקוד — לא היו למשתמש/סוכן פרטי דשבורד/תיעוד Cardcom בזמן הבנייה), כולל מצב `CARDCOM_DRY_RUN=true` שמאפשר לבדוק את כל הזרימה בלי כסף אמיתי. `.env.local.example` + `.env.local` מקומי (dry-run) נוצרו; **המשתמש ימלא בעצמו credentials אמיתיים** (בחר במפורש לא להדביק אותם בצ'אט). `rateLimit.ts` חולץ מ-`/api/lead` לקובץ משותף (`createRateLimiter`/`getClientIp`) ונעשה בו שימוש חוזר גם ב-checkout, במקום לשכפל קוד — `/api/lead` עצמו לא השתנה התנהגותית. אומת מקצה לקצה ב-Playwright עם `CARDCOM_DRY_RUN=true`: alfa→checkout→toggle cross-sell (b600, 5% הנחה מוצג נכון)→טופס→"תשלום"→success page; checkout כספת (`ib250`) עם פירוק משלוח+התקנה נכון; checkout קורס (`lock-installation`) **ללא** cross-sell וללא שדה כתובת, בדיוק לפי ההחלטה; `/checkout/not-a-real-slug` מחזיר 404. `tsc`/`eslint` נקיים. ראה `payments-agent/provider-decision.md` ו-`security-compliance-agent/security-checklist.md` עדכוני 2026-07-10 לפירוט אבטחה/TO-VERIFY המלא לפני כסף אמיתי.
- **עודכן 2026-07-09 (סבב חמישי) — תמונות מוצר אמיתיות + lightbox הורחבו לשאר 3 דגמי המנעולים (Model T/Smart Plus/Smart), ותמונת קטלוג נוספה ל-`locks/page.tsx`.** בקשת המשתמש הייתה מפורשת: "בדיוק כמו ALFA". שני תיקוני קוד קטנים נדרשו פעם אחת ונשארו יציבים לכל 3 הדגמים בלי שינוי נוסף: (1) `locks/page.tsx` — כרטיס קטלוג כל דגם מציג `p.images?.[p.colors[0]?.id]` אם קיים, אחרת גרדיאנט; (2) `ProductHeroGallery.tsx` — תמונות התקנה הן `<button>` שפותח `InstallPhotoLightbox` (מסך מלא, ניווט חצים/מקלדת, סגירה ב-Escape/רקע/✕). שתי התוספות היו גנריות מהיום הראשון (נבנו בסבב ALFA), כך שהוספת 3 הדגמים דרשה **רק דאטה חדשה** ב-`lib/locks.ts` (`images`/`installPhotos` לכל דגם) — אין שינוי קוד/component. תמונות נשמרו תחת `site/public/images/locks/{model-t,smart-plus,smart}/`, כווצו/הומרו ב-`sharp` (כמה קבצי מקור היו מגה-בייטים בודדים, כולל PNG אחד 1.17MB שהומר ל-JPEG). **כעת כל 4 דגמי המנעולים (ALFA/Model T/Smart Plus/Smart) כוללים תמונות אמיתיות + גלריית התקנות + lightbox** — אין עוד gradient placeholder בקטגוריית המנעולים. ראה `product-page-agent/model-t.md`, `smart-plus.md`, `smart.md` לפירוט מקור/קבצים מלא לכל דגם.
- **עודכן 2026-07-13 (סבב אחד-עשר) — `NightHero` (CSS placeholder) הוחלף בוידאו Hero אמיתי במסך מלא, נוצר ב-Higgsfield MCP.** בקשת המשתמש: וידאו הדגמה למנעול חכם שיתפרש על פני **כל** אזור ה-Hero (לא קופסה צדדית), עם הטקסט/כפתורים מונחים מעליו. `HeroVideo.tsx` (client component חדש) — `<video>` עם `object-cover`+`poster`, `aria-hidden` (דקורטיבי מאחורי טקסט משמעותי), autoplay מופעל דרך JS (`video.play()` ב-`useEffect`, לא attribute `autoPlay`) כדי לכבד `prefers-reduced-motion` בלי הבזק תנועה — אם המשתמש ביקש reduced motion, `.play()` פשוט לא נקרא ונשאר ה-poster. `page.tsx` הירו שונה ממבנה grid דו-טורי לפריסת שכבות: `<HeroVideo/>` (רקע מלא) + `<div>` scrim גרדיאנט (כיוון שונה למובייל מול desktop — ראה בהמשך) + תוכן טקסט ב-`relative z`. `NightHero.tsx` נמחק (לא נמצא שימוש נוסף בגרפ). **וידאו ראשון (8 שנ')** נוצר מ-3 תמונות זוויות ALFA/Model T עם Higgsfield `seedance_2_0` (נדחה, 403 — דורש תוכנית Pro/Ultimate) ואז `seedance_2_0_mini` (עבד, זמין ב-starter plan), `image_references` role, `generate_audio:false`. **המשתמש ביקש עדכון** (סרטון שני, 6 שנ', "נוכחות חזקה על המוצר", העלה תמונת Model T כחול בודדת) — נוצר מחדש ואותם קבצי יעד הוחלפו (`public/videos/homepage-hero-model-t-demo.mp4`, `public/images/homepage-hero-model-t-demo-poster.jpg`) כדי ש-`HeroVideo.tsx` לא יזדקק לשינוי קוד. תמונות מקור (`hero source/`) לא ב-git — הועלו ל-Higgsfield דרך `media_upload`+curl PUT+`media_confirm` (widget לא נתמך בסביבה זו, "MCP Apps UI" לא זמין; הודעת fallback של הכלי הנחתה בדיוק את המסלול הזה). **באג CSS קדם-קיים התגלה ותוקן תוך כדי:** `h1,h2,h3{color:navy-deep}` ב-`globals.css` לא היה עטוף ב-`@layer base` → לפי CSS Cascade Layers, כלל לא-מסווג (unlayered) מנצח כל כלל מסווג (`.text-cream` ב-`@layer utilities` של Tailwind) **ללא קשר לספציפיות** — כתוצאה מכך ה-H1 של ההירו (וה-H2 של "מי מייצר, מי מתקין") תמיד עיבדו navy-על-navy, כמעט בלתי נראים, פשוט לא הבחינו בזה כי הרקע היה קודם צבע אחיד. תוקן בעטיפת הכלל ב-`@layer base {}` — אומת חזותית ש-2 הכותרות עברו לקרם, אין שינוי בשאר האתר (רוב הכותרות ללא override צבע כלל). **תיקון נוסף למובייל:** scrim הפוך לכיוון (`bg-gradient-to-b`, לא `bg-gradient-to-l`) מתחת ל-`sm:` — בפריסת עמודה יחידה (מובייל) הכפתורים/badges התנגשו ויזואלית עם המוצר בוידאו; גרדיאנט אנכי כהה-יותר-למטה נתן הפרדה נקייה בלי לפגוע בנוכחות המוצר למעלה. אומת: `tsc`/`eslint` נקיים, Playwright desktop(1440)+mobile(390) לפני/אחרי כל תיקון, `video.paused===false` אחרי mount (autoplay פעיל), אין console errors/warnings. וידאו סופי: 6 שנ', 668KB, ללא אודיו, H.264+faststart.
- **עודכן 2026-07-10 (סבב עשירי) — 3 תיקוני checkout לפני מסירת פרטי Cardcom: cross-sell תמיד ALFA לכספות, אין cross-sell למנעולים/קורס, ואיסוף עצמאי לכספות.** `lib/crossSell.ts` נכתב מחדש — רק כספות (נשק+בית) מציעות cross-sell, תמיד `alfa` ("המנעול המתקדם ביותר בישראל"), לא זיווג לפי מחיר יותר; מנעולים וקורס לא מציעים cross-sell בכלל (הוסרו לגמרי מהמפה, לא רק "לא נוספו"). `lib/checkout.ts` קיבל `PICKUP_LOCATION`/`FulfillmentMethod` — `CheckoutFlow.tsx` מציג radio "משלוח עד הבית"/"איסוף עצמאי מאולם התצוגה" רק לכספות (`isSafe`); באיסוף עצמאי שדה הכתובת והתקנה מקצועית נעלמים (הנחת עיצוב: התקנה דורשת הגעת טכנאי לכתובת, לא רלוונטי עם איסוף עצמאי — לא אושרה מפורשות מול המשתמש, ראה `payments-agent/provider-decision.md` להערה). `/api/checkout` אוכף הכל בשרת — מתעלם מ-`includeInstallation` אם `fulfillment==="pickup"`, לא דורש כתובת. אומת: `tsc`/`eslint` נקיים, curl (כולל ניסיון תקיפה) ו-Playwright חזותי. ראה `payments-agent/provider-decision.md` עדכון 2026-07-10 (סבב שלישי) לפירוט המלא.
- **עודכן 2026-07-13 (סבב שנים-עשר) — code review מקיף (לא QA פונקציונלי, נעשה במקביל בסוכן נפרד) על checkout/Cardcom + HeroVideo + globals.css layer fix + catalogBadge, לפי בקשת המשתמש.** `tsc --noEmit` ו-`eslint . --max-warnings=0` הורצו בפועל — שניהם נקיים. **נמצא ותוקן במקום (תיקון טריוויאלי וחד-משמעי):** המשימה תיארה עדכון ל-`lib/locks.ts` שכביכול כבר הוסיף `catalogBadge: "נבחר במיוחד עבור הדלת הישראלית"` ל-alfa/model-t/smart (בהתאמה ל-smart-plus שכבר היה לו) — בפועל הקוד הראה ש**רק smart-plus** (שורה 334 המקורית) קיבל את השדה; שלושת האחרים היו `undefined`, כך שרק כרטיס אחד מתוך 4 ב-`/locks` הציג את התג ב-`ProductListCard.tsx`. תוקן: אותו מחרוזת בדיוק נוספה ל-alfa/model-t/smart (עכשיו שורות 113/229/438 — smart-plus זז ל-336), אומת עם grep ש-4/4 מוצרים כוללים את השדה כעת. `ProductListCard.tsx`/`ProductHeroGallery.tsx` עצמם היו תקינים מלכתחילה (רינדור מותנה נכון) — הבאג היה רק בדאטה. **ממצא לא-מתוקן, חומרה גבוהה (רק דיווח, נדרשת החלטת עיצוב):** `colorId` שנבחר ב-`CheckoutFlow.tsx` (state + query param מ-`BuyNowButton`) נשלח ב-body של `POST /api/checkout` (`checkout/[slug]`, שורה ~95) אבל **אף פעם לא נקרא** ב-`api/checkout/route.ts` — לא נשמר ב-`PendingOrder` (`orderStore.ts`, אין שדה `colorId` בטיפוס), לא מופיע ב-`console.log`/Cardcom description. משמעות בפועל: לקוח שבוחר "שחור" ל-ALFA — בחירת הצבע נעלמת לפני שהיא מגיעה לרשומת ההזמנה, וצוות ההתקנה/מילוי לא יידע איזה צבע לספק. **ממצא לא-מתוקן, חומרה בינונית:** חישוב הסכום (כלל "משלוח כלול בהתקנה" + הנחת cross-sell) ממומש פעמיים באופן עצמאי — `CheckoutFlow.tsx` (שורות 65-81, לתצוגה) ו-`api/checkout/route.ts` (שורות 75-107, מקור האמת לחיוב בפועל) — כרגע זהים לוגית, אך אין הפשטה משותפת; שינוי עתידי באחד בלי השני יגרום לפער שקט בין הסכום שהלקוח רואה למה שנגבה בפועל. **ממצא קל:** `success/page.tsx` (שורות 31-34) מציג תמיד "ניצור קשר לתיאום המשלוח וההתקנה" לכל פריט פיזי, גם ללקוח שבחר איסוף עצמאי וללא התקנה — לא קורא את `pendingOrders`/`order` query param כדי להתאים את הניסוח. **אומת כתקין (לא באג):** `formatPrice(item.installationPrice!)` ב-`CheckoutFlow.tsx:285` — ה-`!` בטוח בפועל כי הביטוי מרונדר רק בתוך `{showInstallation && (...)}` ו-`showInstallation` כבר כולל `item.installationPrice !== undefined`; TypeScript פשוט לא מצליח לצמצם טיפוס דרך משתנה בוליאני נגזר, לא חור אמיתי. **`HeroVideo.tsx`:** תבנית ה-`useEffect`+`matchMedia`+`.play()` תקינה וללא race — אין mismatch של hydration (אין `autoPlay` attribute ב-SSR), אין תלות שדורשת cleanup (אין listener/timer שנרשם). **`globals.css`:** ה-fix של `h1,h2,h3` בתוך `@layer base` נכון ומלא. נמצאו כללים גלובליים נוספים באותה צורה (unlayered): `html{direction:rtl}` ו-`body{background/color/font-family}` (שורות 41-49) ו-`.num{}` — כרגע לא גורמים לבאג נצפה כי אין class Tailwind מתנגש שמופעל בפועל על `<body>`/עם `.num`, אבל זו אותה "מוקש" בדיוק: אם מישהו יוסיף בעתיד `className="bg-..."` על ה-`<body>` ב-`layout.tsx` בציפייה שתגבר, היא תפסיד בשקט. לא תוקן מיידית (אין באג פעיל, שינוי מונע גרידא) — מומלץ לעטוף גם אותם ב-`@layer base` בסבב הבא. `crossSellMap` (6 מפתחות: model-f/model-e/ib250/sc250/bsa250/b600) אומת מול `lib/safes.ts`/`lib/homeSafes.ts` בפועל — כל ה-slugs קיימים, אין typo.
- **עודכן 2026-07-09 (סבב שלישי) — עמוד הקורס `courses/lock-installation` שוכתב במלואו סביב סילבוס אמיתי שסופק (PDF).** 7 פרקים/24 שיעורים, מבחן אמריקאי, תעודת מתקין מוסמך, ליווי אישי בבית לקוח, 3 הטבות בוגר (מחירון מתקין, הפניית לקוחות, תמיכה טכנית). הוסרה עובדה מומצאת מהגרסה הקודמת ("3 ימי הכשרה", לא הייתה מבוססת מקור) והוסר כרטיס קורס פיקטיבי שני מ-`courses/page.tsx` (slug כפול, לא route אמיתי). **החלטת מיתוג:** מקור הסילבוס ממותג "TECH.PRO" (כולל טענת "לראשונה בישראל") — **לא** הועתק מיתוג/טענת-על זו לאתר; כל התוכן מוצג תחת המוקד בלבד, עקבי עם עמדת "מותג אחד" ב-CLAUDE.md. יש לברר מול המשתמש אם TechPro הוא שותף תוכן בפועל שדורש קרדיט גלוי. אין `lib/*.ts` חדש — קורס יחיד עדיין מוצדק כ-route סטטי, לא דינמי. ראה `product-marketing-strategy-agent/courses.md` ו-`product-page-agent/courses.md` לפירוט המלא (כולל מחיר קורס שטרם נקבע, מספר ימי ליווי לא צוין במקור).
- **עודכן 2026-07-14 (סבב שלושה-עשר) — בלוג/מאמרים חדש (`/blog` + `/blog/[slug]`) + סוכן חדש `blog-agent`, לפי בקשת המשתמש.** נבדק מראש (2 סבבי Explore + סבב Plan): אין CMS/DB/markdown בפרויקט כלל; אושר עם המשתמש קובץ MDX נפרד לכל מאמר (לא מערך TS משותף כמו `locks.ts` — טקסט ארוך מתאים פחות לצורת ה-block-array של הקטלוגים). **תשתית:** הותקנו `@next/mdx`+`@mdx-js/loader`+`@mdx-js/react`+`@types/mdx`+`remark-frontmatter`+`remark-mdx-frontmatter`+`remark-gfm`+`@tailwindcss/typography` (הצירוף המתועד ב-`node_modules/next/dist/docs/01-app/02-guides/mdx.md`, אומת בפועל). `next.config.ts` עטוף ב-`createMDX` עם **שמות plugin כמחרוזות** (לא function refs מיובאים) — נדרש לתאימות Turbopack ("JS functions can't be passed to Rust", מתועד). `src/mdx-components.tsx` (חובה לפי Next docs, האתר לא יעבוד בלעדיו) ממפה `img`→`next/image` בתוך `<span>` (לא `<div>` — markdown `![]()` מרונדר בתוך `<p>`, שלא יכול להכיל `<div>`). `.prose-brand` ב-`globals.css` ממפה את משתני `--tw-prose-*` של `@tailwindcss/typography` לטוקנים הקיימים של המותג (`--color-navy-deep` וכו') — לא פלטת ברירת מחדל אפורה. **מבנה:** `src/content/blog/<slug>.mdx` (קובץ אחד=מאמר אחד), `src/lib/blog.ts` (`getAllBlogSlugs`/`getAllBlogPosts`/`getBlogPost` — אותו "חוזה" קריאה כמו `getLockBySlug`, כדי ש-`page.tsx` לא יצטרך לדעת שהמקור MDX ולא מערך), `src/components/BlogPostCard.tsx` (קומפוננטה חדשה, לא נכפה על `ProductListCard` — ה-props שלו מחיר/צבע-ספציפיים ולא מתאימים). `generateStaticParams`+`dynamicParams=false` ב-`/blog/[slug]`; `generateMetadata` על בסיס התבנית הקיימת מ-`lp/[slug]/page.tsx` אך **בלי** `robots:{index:false}` (מאמרים כן צריכים אינדוקס, בניגוד ל-lp). קישור "בלוג" נוסף ל-`Header.tsx`. **באג אמיתי שהתגלה ותוקן תוך כדי (לא נצפה בתכנון):** `import(\`@/content/blog/${slug}.mdx\`)` הוא הדפוס המתועד הרשמי, אבל אם `src/content/blog/` **ריקה לגמרי** — Turbopack פותר את התבנית הדינמית הזו **בזמן קומפילציה**, לפני שקוד ה-runtime רץ, ונכשל אם אין אף קובץ תואם → כל `/blog` (ולמעשה כל build) קורס ב-500, גם שהלוגיקה ב-`getAllBlogPosts()` הייתה מחזירה מערך ריק תקין. תוקן בקובץ קבוע `src/content/blog/_placeholder.mdx` (`draft:true`, לעולם לא נמחק — מתועד בהערת header בקובץ עצמו ובקובץ `blog-agent.md`) ששומר על התיקייה לא-ריקה גם כשאין עדיין מאמרים אמיתיים. אומת: `npm run build` מלא (לא רק dev) רץ פעמיים — עם 2 מאמרי בדיקה (אחד draft) ואז שוב עם placeholder בלבד — שני המקרים עברו. Playwright desktop+mobile, `curl` לאימות 404 על draft. `tsc`/`eslint` נקיים. מאמרי הבדיקה נמחקו לפני סיום (לא תוכן אמיתי). **סוכן חדש `.claude/agents/blog-agent.md`** (פורמט זהה ל-21 הסוכנים הקיימים) — מתועד בו במפורש: gate חובה של `security-compliance-agent` לתוכן כספות-נשק, איסור ערבוב "Techom פיתחה" מול "המוקד פיתחה", חובת `npm run build` לפני דיווח סיום, ואזהרת ה-`_placeholder.mdx`. חובר כ-`blog` argument חדש ב-`.claude/skills/brand-promote/SKILL.md` (לצד `content`/`native` הקיימים). **לא ב-MVP (במפורש):** פילטר קטגוריה, pagination, `sitemap.ts` (פרויקט full-site נפרד), RSS, שדה `tags`, בייליינים אישיים. ראה `.claude/agents/blog-agent.md` ו-`.claude/skills/brand-promote/SKILL.md` לפירוט המלא.
- **עודכן 2026-07-17 — עמוד פנימי חדש `/links` ("מפת קישורים"), לפי בקשת המשתמש שרצה רשימה שמתעדכנת אוטומטית כשכתובת האתר משתנה.** לא קובץ סטטי/memory — עמוד אמיתי באתר (`src/app/links/page.tsx`) שקורא את `host`/`x-forwarded-proto` דרך `await headers()` (אושר מול `node_modules/next/dist/docs/01-app/03-api-reference/04-functions/headers.md` שזו פונקציה async ב-Next הזה) ובונה את ה-base URL מהבקשה האמיתית בזמן ריצה — לא מ-`NEXT_PUBLIC_SITE_URL` (שגילינו שנשאר `localhost:3000` גם אחרי הפריסה בפועל ל-Vercel, ולכן לא אמין לצורך הזה). כל הסקציות נשאבות ישירות מ-`lib/locks.ts`/`homeSafes.ts`/`safes.ts`/`courses.ts`/`blog.ts`/`landingPage.ts` הקיימים — אין רשימת slugs כפולה/מקובעת, כך שמוצר/מאמר חדש מופיע אוטומטית. `robots:{index:false,follow:false}` (כלי פנימי, לא תוכן) ו**לא** מקושר מ-`Header.tsx` (כדי לא לבלגן ניווט ללקוחות עם 40+ קישורים) — נגיש רק בגישה ישירה ל-`/links`, אותו דפוס בדיוק כמו `/lp/*`. אומת: `npx tsc`/`eslint` נקיים, `npm run build` מלא עבר (מוצג כ-`ƒ` dynamic ב-build output, כצפוי בגלל `headers()`), Playwright אישר בפועל שכל 9 הסקציות ו-40 הקישורים מרונדרים (החפיפה הוויזואלית עם ה-sticky header ב-full-page screenshot הייתה artifact של הצילום, לא באג — אומת דרך DOM ישירות). נבדק גם מול production Vercel (`moked-israel.vercel.app`) שהדומיין באמת עובד לפני בניית העמוד הזה.