# בריף דף מוצר — Model T

> נבנה על ידי product-page-agent, יולי 2026. **עודכן במלואו 2026-07-08** — קטלוג PDF רשמי של Techom ל-Model T נמסר, וכל התוכן הישן במסמך זה (מבוסס על 3 שיטות פתיחה בלבד, ותכנון לרכיבים שטרם נבנו) הוחלף. מבנה האתר בפועל כבר התקדם משמעותית מאז הגרסה הראשונה של מסמך זה: `ModelComparisonTable`, `ProductFAQ`, `bestFor`, `problemStatement`, `benefitTranslations` ו-`faq` כבר קיימים כשדות ב-`LockProduct` וכרכיבים גנריים ב-`page.tsx` (נבנו בסבב ALFA) — **אין צורך לבנות רכיבים חדשים ל-Model T, רק לעדכן דאטה.**
>
> **הערת אימות זיהוי כף יד:** הקטלוג כותב במפורש "שישה אמצעי גישה" (זיהוי פנים, טביעת אצבע, קוד אישי, צ'יפ, אפליקציה, מפתחות לגיבוי) — בלי לנקוב "זיהוי כף יד" בטקסט. נוסף כאמצעי גישה 7 לפי אישור מפורש של המשתמש בשיחה (2026-07-08), לא ציטוט מילולי מהקטלוג. ראה `product-marketing-strategy-agent/model-t.md` להקשר המלא.

## מה שונה בפועל בקוד (2026-07-08)
`lib/locks.ts` — Model T עודכן במלואו: `tagline` ("פרימיום ל-150 משתמשים — 7 אמצעי גישה, מצלמה מובנית ואזעקה"), `description`, `highlights` (מ-4 ל-6 פריטים: 7 אמצעי גישה + 150 משתמשים, מצלמה+וידאו+צג, אזעקה מובנית, כפתור שבת+פעמון, עמידות מזג אוויר+סוללה+התרעות עבריות, פתיחה מרחוק+היסטוריה+קודים זמניים), `features` (כל 9 הדגלים הפכו ל-`true`), `benefitTranslations` (6 פריטים תואמים ל-highlights), `faq` (5 שאלות — כולל שאלה חדשה "מה ההבדל בין Model T ל-ALFA").

`ModelComparisonTable` **נגזר אוטומטית** מ-`features` בקוד — לא נדרש שינוי קוד נפרד שם, הטבלה כבר מציגה ✓ עבור Model T בכל השורות הרלוונטיות ברגע שהדאטה עודכן.

**השפעת צד על ALFA:** Model T התברר ככמעט-זהה ל-ALFA ביכולות (זיהוי פנים, כף יד, מצלמה, אזעקה, שבת) — לכן `bestFor` וה-FAQ של ALFA עודכנו במקביל (ראה `product-page-agent/alfa.md`) כדי לא לטעון עוד ש-ALFA "היחיד" עם זיהוי פנים/מצלמה. ה-USP שנשאר ל-ALFA: ללא הגבלת משתמשים (מול 150 ב-Model T).

## מבנה הדף בפועל (Model T עובר באותו component דינמי כמו ALFA — page.tsx לא שונה)
1. הירו — שם, tagline מעודכן, `PriceDisplay`, `ProductOrderForm`+CTA לוואטסאפ.
2. `highlights` — 6 פריטים.
3. `problemStatement` — נשמר הרעיון הקיים ("מה קורה כשנגמרת הסוללה באמצע הלילה") אך הגוף הורחב לאזכר 7 אמצעי גישה ומפתח פיזי, לא רק "גיבוי מכני".
4. `benefitTranslations` — 6 סעיפים ממופים ישירות ל-highlights.
5. **אין `bestFor` נפרד ל-Model T** — ההשוואה מול ALFA מכוסה כבר ב-FAQ ("מה ההבדל בין Model T ל-ALFA?"), אין צורך בכפילות.
6. `ModelComparisonTable` — data-driven, ללא שינוי קוד.
7. הוכחה חברתית — אין תוכן אמיתי, נשאר TODO בקוד כמו בשאר הדגמים. טענת "עשרות אלפי לקוחות" בקטלוג היא נתון Techom ברמת היצרן — לא נעשה בה שימוש כעדות של המוקד.
8. `faq` (`ProductFAQ`) — 5 שאלות.
9. CTA סיום + טופס — `ProductOrderForm` הקיים, ללא שינוי.

## הנחיות ל-web-dev-agent
לא נדרש קוד/component חדש. כל התשתית (`ModelComparisonTable`, `ProductFAQ`, `PriceDisplay`, `ProductOrderForm`, `page.tsx` הדינמי) כבר קיימת ונבנתה גנרית סביב `lib/locks.ts` בסבב ALFA (2026-07-08 מוקדם יותר באותו יום) — זהו עדכון דאטה בלבד. אומת: `tsc --noEmit` ו-`eslint .` נקיים.

## דגלים לפני שקה בפועל
- ~~תמונת מוצר אמיתית ל-Model T~~ — **נפתר 2026-07-09**, ראה עדכון למטה.
- אישור מספר שנות אחריות סופי מול security-compliance-agent — עדיין פתוח (משותף לכל 4 הדגמים).
- **טרם התקבלו קטלוגי Smart Plus / Smart** — features/highlights שלהם לא שונו, גם אם ALFA/Model T מרמזים על יכולות דומות. אין להסיק אנלוגית.
- זיהוי כף יד ב-Model T מבוסס אישור משתמש בשיחה, לא ציטוט קטלוגי מילולי — לחזור ולבדוק מול Techom אם מתעורר ספק לפני פרסום ממומן.

---

## עדכון 2026-07-09 — תמונות מוצר אמיתיות (אותה תבנית שנקבעה ב-ALFA, `alfa.md` עדכון 4-5)

מקור: תיקיית Google Drive `המוקד/MODEL T` — 7 קבצי תמונה + PDF (דולג). 2 "תמונה ראשית" ברקע לבן (כחול מאט, אפור-שחור) ו-5 תמונות התקנה אמיתיות על דלתות לקוחות (שמות UUID אקראיים מה-Drive).

נשמר תחת `site/public/images/locks/model-t/`: `main-blue-matte.jpg` + `main-gray-black.jpg` (כווצו ל-1000px, JPEG 88), `install-1.jpg`..`install-5.jpg` (כווצו ל-1200px מקסימום איפה שנדרש, JPEG 82).

`lib/locks.ts` קיבל `images: { blueMatte, grayBlack }` ו-`installPhotos` (5 נתיבים) על ערך ה-Model T. **לא נדרש שינוי קוד** — הארכיטקטורה (`ProductHeroGallery`, lightbox, כרטיס קטלוג ב-`locks/page.tsx`) כבר הייתה גנרית מסבב ALFA. אומת חזותית ב-Playwright: `/locks` מציג תמונת Model T כחולה אמיתית בכרטיס; `/product/model-t` מציג את 5 תמונות ההתקנה, lightbox נבדק בפועל (קליק על תמונה שנייה פתח אותה במסך מלא).